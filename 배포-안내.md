# 컬러비즈 도안 갤러리 — 배포 안내

저장소는 GitHub, 호스팅은 GitHub Pages, 재고는 구글 스프레드시트에 둡니다.
결제 수단 등록이 필요 없고, 모든 태블릿이 같은 재고를 봅니다.

작업 순서는 재고 서버(1단계)를 먼저 만들고, 그 주소를 들고 저장소(2단계)로 갑니다.
전체 소요는 처음 하실 때 30분 내외입니다.

---

## 1단계 · 재고 서버 만들기 (구글 스프레드시트)

1. 구글 드라이브에서 새 스프레드시트를 만들고 이름을 `컬러비즈 재고` 로 둡니다.
2. 메뉴에서 확장 프로그램 > Apps Script 를 엽니다.
3. 편집기에 있던 내용을 모두 지우고 `apps-script/Code.gs` 의 내용을 붙여넣은 뒤 저장합니다.
4. 편집기 상단 함수 목록에서 `초기설정` 을 고르고 실행합니다.
   권한 승인 창이 뜨면 본인 계정으로 승인합니다. 스프레드시트에 `재고` 와 `소비기록` 두 시트가 생깁니다.
5. 파일 > 프로젝트 설정 > 스크립트 속성에서 속성 하나를 추가합니다.
   - 속성 이름: `API_KEY`
   - 값: 임의의 문자열 20자 이상 (예: `bead-2026-widuk-a7f2k9`)
6. 배포 > 새 배포 > 유형 선택에서 웹 앱을 고르고 다음과 같이 설정합니다.
   - 실행 계정: 나
   - 액세스 권한이 있는 사용자: 모든 사용자
7. 배포 후 나오는 웹 앱 URL(`https://script.google.com/macros/s/.../exec`)을 복사해 둡니다.

학교 계정 정책 때문에 액세스를 모든 사용자로 둘 수 없다면 `uu.ac.kr의 모든 사용자` 로 두십시오.
그 경우 태블릿이 학교 계정으로 로그인되어 있어야 재고가 연결됩니다.

확인 방법: 복사한 주소 뒤에 `?key=설정한값` 을 붙여 브라우저에서 열면 재고가 JSON으로 나옵니다.

---

## 2단계 · 저장소 만들기 (GitHub)

1. GitHub에서 새 저장소를 만듭니다. 이름 예시는 `beads-pattern-gallery` 이며 공개·비공개 모두 가능합니다.
2. 프로젝트 폴더 전체를 올립니다. 브라우저에서 파일을 끌어다 놓아도 되고, 아래 명령을 써도 됩니다.

```bash
git init
git add .
git commit -m "컬러비즈 도안 갤러리 초기 배포"
git branch -M main
git remote add origin https://github.com/86loading-droid/beads-pattern-gallery.git
git push -u origin main
```

3. 저장소 Settings > Secrets and variables > Actions 에서 New repository secret 을 두 번 눌러 등록합니다.
   - `VITE_STOCK_API` : 1단계에서 복사한 웹 앱 URL
   - `VITE_STOCK_KEY` : 1단계에서 정한 API_KEY 값
4. Settings > Pages 에서 Source 를 GitHub Actions 로 바꿉니다.
5. Actions 탭에서 배포가 끝나면 주소가 나옵니다.
   `https://86loading-droid.github.io/beads-pattern-gallery/`

`.env` 파일은 저장소에 올리지 않습니다(`.gitignore`에 이미 제외되어 있습니다).
비밀값은 Secrets 로만 넣으십시오.

---

## 3단계 · 태블릿에 올리기

배포 주소를 태블릿 브라우저에서 열고 홈 화면에 추가하면 앱처럼 열립니다.

- 아이패드 사파리: 공유 버튼 > 홈 화면에 추가
- 안드로이드 크롬: 우측 상단 점 세 개 > 홈 화면에 추가

여러 대에서 동시에 완성 버튼을 눌러도 서버가 순서대로 처리하므로 수량이 어긋나지 않습니다.
화면 상단에 현재 연결 상태(스프레드시트 공유 재고 또는 연결 끊김)가 표시됩니다.

---

## 이후 관리

도안을 추가할 때는 `src/data/patterns.ts` 에 한 덩어리를 넣고 저장소에 올리면 됩니다.
몇 분 뒤 자동으로 다시 빌드되어 태블릿에 반영됩니다. 작성 형식은 `src/data/도안-추가-방법.md` 에 있습니다.

재고는 스프레드시트의 `재고` 시트에서 직접 고쳐도 됩니다. 앱은 30초마다 다시 읽으므로
새로 들어온 비즈를 시트에 입력하면 태블릿에도 반영됩니다. 반대로 앱의 재고 수량 수정으로
고친 값도 시트에 그대로 기록됩니다.

소비 기록은 `소비기록` 시트에 쌓이며 최근 300건을 보관합니다. 더 오래 남기려면
`Code.gs` 의 `LOG_KEEP` 값을 올리십시오. 학기 단위로 정산하실 경우 시트를 복사해 보관하시면 됩니다.

---

## 알아 두실 점

접속 열쇠(`VITE_STOCK_KEY`)는 브라우저에서 페이지 소스를 열면 보입니다. 아무나 주소만 알고
재고를 건드리는 일을 막는 수준이며, 암호로서의 보호력은 없습니다. 이 앱이 다루는 것은
비즈 수량뿐이고 학생 개인정보나 성적은 저장하지 않으므로 이 수준으로 충분하다고 보았습니다.
더 엄격한 잠금이 필요하시면 웹 앱 액세스를 학교 계정 사용자로 제한하는 방법이 있습니다.

재고 서버에 연결하지 못하면 화면 상단에 연결 끊김이 표시되고 차감이 저장되지 않습니다.
이때는 수업 후 재고 수량 수정으로 실제 사용량을 한 번에 반영하시면 됩니다.

Apps Script 웹 앱은 동시 실행에 상한이 있습니다. 태블릿 열 대 안팎에서는 문제가 없으나,
전원이 같은 순간에 완성 버튼을 누르면 잠깐 대기가 생길 수 있습니다.
